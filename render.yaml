services:
  - type: web
    name: daryls-declaration-decoder
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd server && python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
